# frozen_string_literal: true

RSpec.describe Ruslanspivak do
  it "has a version number" do
    expect(Ruslanspivak::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
